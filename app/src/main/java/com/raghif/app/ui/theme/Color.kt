package com.raghif.app.ui.theme

import androidx.compose.ui.graphics.Color

// Humanitarian-utility palette: high contrast, sunlight-readable, works on cheap/older
// screens. One trustworthy accent (deep blue), near-black text, semantic status colors
// that don't rely on hue alone (paired with icons/labels for colorblind-safety).

val DeepBlue = Color(0xFF1266F1) // bright, saturated — a clear "this is tappable" blue
val DeepBlueDark = Color(0xFF5B9CFF) // accent on dark backgrounds

val SecondaryFill = Color(0xFF344055) // solid slate — secondary actions, still a real button not an outline
val SecondaryFillDark = Color(0xFF4A5A75)

val Ink = Color(0xFF161616)
val InkMuted = Color(0xFF4A4A4A)
val Paper = Color(0xFFFAFAF7)
val Surface = Color(0xFFFFFFFF)
val OutlineGray = Color(0xFFB0B0AA)

val Success = Color(0xFF2E7D32)
val SuccessOn = Color(0xFFFFFFFF)
val SuccessContainer = Color(0xFFDCEFDD)

val Warning = Color(0xFF9A6300)
val WarningOn = Color(0xFFFFFFFF)
val WarningContainer = Color(0xFFFCE9C6)

val Danger = Color(0xFFB3261E)
val DangerOn = Color(0xFFFFFFFF)
val DangerContainer = Color(0xFFF9DEDC)

val InkDark = Color(0xFFECECEC)
val PaperDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1C1C1C)
